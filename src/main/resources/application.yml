# Conversion to YAML from Properties formar report
# Warnings:
# - The yaml file had comments which are lost in the refactoring!
spring:
  application:
    name: restclient
  mvc:
    view:
      suffix: .html
  boot:
    admin:
      client:
        url:
          http://localhost:1111
rest_client:
  passenger:
    url: http://localhost:8080/passenger
  bookticket:
    url: http://localhost:8080/bookTicket
  getticket:
    url: http://localhost:8080/getTicket/{ticketId}
